[user]
  email = danielboll.academico@gmail.com
  name = Daniel Boll
  signingkey = A83245AD141A70E3
[gpg]
  program = gpg2
[mergetool "nvim"]
  cmd = nvim -d $LOCAL $REMOTE $MERGED -c '$wincmd w' -c 'wincmd J'
[merge]
  tool = nvim
[mergetool]
  prompt = false
[init]
	defaultBranch = main
[gc]
	reflogExpire = 200
	reflogExpireUnreachable = 90
[alias]
	lg = log --graph --abbrev-commit --decorate --date=format:'%Y-%m-%d %H:%M:%S'\n    --format=format:'%C(bold blue)%h%C(reset) - %C(bold cyan)%ad%C(reset) %C(bold green)(%ar)%C(reset)%C(bold yellow)%d%C(reset)%n''\n    %C(white)%s%C(reset) %C(dim white)- %an%C(reset)'
