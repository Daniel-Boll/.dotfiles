
function a(t, h, m) { if (t === "00:00") return "not complete"; let [a, b] = t.split(":").map(Number), n = a * 60 + b - (h * 60 + m); n < 0 && (n = 1440 + n); let x = Math.floor(n / 60), y = n % 60; return `${String(x).padStart(2, "0")}:${String(y).padStart(2, "0")}` } let lines = document.querySelector(".ui-datatable-data").children; for (let i = 1; i < lines.length; i++) { let line = lines.item(i).children, date = line.item(0).children[0].children[0].innerText, hour = line.item(line.length - 2).children[0].children[0].innerText, newHour = a(hour, 1, 48); console.log(`${date} - ${newHour}`) }
